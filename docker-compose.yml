version: '3.9'

services:
  ai-company:
    build: .
    ports:
      - "8000:8000"

  postgres:
    image: postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: aicompany
    ports:
      - "5432:5432"